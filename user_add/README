1 Add user id to "users" file in order to create or delete user
2 Execute "checkusers.py", it will generate two files "push_users.sh" and "delusers.sh"
3 If there is need to delete users then execute "delusers.sh" and if there is need to create users then execute "push_users.sh"
